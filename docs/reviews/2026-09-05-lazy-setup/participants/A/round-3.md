# 参与者 A：第三轮最终投票
- 参与者：A
- 轮次：3
- 状态：submitted
- 选票版本：R3-v1
- 选票内容标识：07ddacf696de08e77b2a00bc0cb2d267704668de
- 内容标识验证：已实测（`git hash-object docs/reviews/2026-09-05-lazy-setup/round-3-ballot.md` → 07ddacf696de08e77b2a00bc0cb2d267704668de）
- 已读输入与未验证事项

已读：`README.md`、`docs/DECISIONS.md`、`docs/interviews/2026-09-04-founding-interview.md`；`participants/A|B|C/round-2.md`；`round-2-summary.md`；`round-3-ballot.md` 全文 19 题。未读任何人 round-3。未搜 vault。Matt 仍以本机 `<USER_SKILLS_DIR>/setup-matt-pocock-skills\SKILL.md` L4/L15/L63–70/L76–82 与 `code-review\SKILL.md` L13 为准；B 的 `~/.claude` 哈希、C 的 `~\.agents` 路径**未**当自己实测。摘要倾向不是决议。

---

## 对第二轮自案的最终修订

- **V16 改 SKILL，撤回把 UX 写入 §2.3。** A-02-r2「仓库已有事实不得询问」对不可靠探测过强，会逼猜，违 7.5/7.6；V04=NEEDED 已够。UX 用规格+SKILL.md 强制，不开会改条文。
- **V12 改 TRACKED，撤回 EITHER。** 采纳 B round-2 §1.3：裸 `.git/hooks` 不可审查、不可随仓走（git 常识，非新实测）。有命令时写受控入口+说明 hooksPath；无命令走 V15=A，不把「无 package.json」写成装不了 hook。
- **V08 仍投 FINGERPRINT，但不把位置/范围冒充已通过机制。** 第二轮未写清；本票只作规格约束（见 V08 理由），他人未同文投过，不能当决议。
- **保留：** V15=A（诚实 hook 状态，不改 6.1）；V06=A（有效 tracker）；提问无硬顶；脏仓精确改；完成语分过程/生效；跨设备与 MCP 延后；薄平台段。
- 未通过前遵守现行 2.3（仍须尝试写 hook）。

---

## 逐项投票

| 票号 | 唯一选择 | 理由与原文依据 | 依赖/阻断项 |
|---|---|---|---|
| V01 | YES | 访谈 Q18；README L5–L8；A-03。脚本是否存在由 V12 定，不因反 CLI 禁掉。 | 无 |
| V02 | ITEMS | A D07；2.3 是六类产物不是六文件。FILES 会裁掉 TRACKED 所需受控脚本。 | 与 V12=TRACKED 同向；与 FILES 互斥 |
| V03 | YES | A-15-r2。可编辑源=本仓 DECISIONS；快照只作编译输入。 | 规格须禁目标仓绝对路径指针 |
| V04 | NEEDED | 访谈 L19 可查不问；7.5 未验证不猜。TWO 超出两项时只能猜或弃未知，违现行。 | 与 V16=RULE 字面冲突；本票 V16=SKILL |
| V05 | YES | 访谈 Q2；Matt L63–70 本机已核。无变化重跑不再问。 | 无 |
| V06 | A | code-review L13 本机已核只查 tracker；空文件不算。B 双检过严（半配置可规格列实例，不入固定层）。C 加 domain.md 无访谈依据。 | 不代跑 Matt |
| V07 | SKILL | 1.3「取已存在者」已够；跟随 Matt L76–78 写进 skill。RULE 补丁非 B 全文且把 CLAUDE 优先写入固定层，访谈未拍这一句。 | 依赖 V06；不改 1.3 |
| V08 | FINGERPRINT | 版本戳不能证未手改（A round-2）。**位置：**各托管块起始标记行内 HTML 注释，同文件、无侧车。**范围：**起止标记之间、排除该元数据行的 UTF-8 正文做内容指纹；四项门禁命令作标记明文属性，命令变即使正文哈希同也走升级。块外不进哈希。REGENERATE 依赖 AI 稳态重写，prompt-driven 未证（摘要不足 2）。 | 位置/范围是规格约束非已通过测试；与「只靠版本戳」互斥 |
| V09 | A | 4.3 不猜；无标记=未托管，确认时选跳过或包标记。B 暂停整份文件可执行但把 RELEASE 人工稿一刀切。C「自愈」范围未证。 | 损坏标记=drift，不自动修 |
| V10 | PRECISE | 整仓干净反 lazy。CLEAN 把无关脏当失败。不授权 checkout 用户改动。 | 与 V11=GIT 互斥 |
| V11 | REPORT | 7.6；不验收原子写盘（Matt L15）。GIT 依赖 V10=CLEAN 且 checkout 误伤未跟踪新文件。 | 先新后改；无 git clean |
| V12 | TRACKED | 修订 A-09-r2。有管理器则追加；否则受控脚本+确认后说明 hooksPath。文件在≠本机已激活。 | 与 LOCAL 互斥；无命令时不写假 hook（V15=A） |
| V13 | CONNECT | 不把 setup 变成装工具。空代码仓未接线+不得称生效，不是推荐安装。RECOMMEND 属 2.4 日后扩展。 | 与 V14=A、V15=A 同向 |
| V14 | A | 过程结束≠门禁生效。不要求 setup 跑满套测试。B 的 wired=实跑仍≠ hook 已被 Git 激活（选票共同验收）。C 易用文件存在冒充生效。 | 规格须分列：命令存在 / 本次实跑 / hook 激活，三者不可互推 |
| V15 | A | 忠实 A-02-r2 hook 段；Q14「先尝试 hook」。不改 6.1/6.4。B 在「无可跑命令」时 RELEASE 记什么未定义。C「无安装条件」会回滑到已否的 Husky 因果。KEEP 逼文档仓说谎或假失败。 | 固定层修订；与 B/C 原文互斥 |
| V16 | SKILL | 修订 A-02。V04/V05 已约束 UX；RULE「不得询问」过强。编译 UX 入条文则每次调提问都要 §9。 | 与 RULE 互斥；不依赖 V15 拼接 UX |
| V17 | DEFER | Q11/Q6 已确认；C-04 已撤。GUIDE 仍是新操作约束，rebase≠文件锁。 | 不改 1.2/3.3/§4 |
| V18 | THIN | 未定项 L131 不是首版门槛。NPM 把无仓库实测写成交付条件，假验收。 | 延后 harvest/上传 |
| V19 | YES | A-13-r2。讨论新建专属路径；实现可改授权文件；收尾不绕 §9。 | 机制未写清前不派实现 |

---

## 固定层版本票

0.2.0：YES。仅当存在已通过的固定层补丁（本票预期仅 V15=A；V07/V16 为 SKILL 则不改 1.3/2.3 UX）。澄清非 breaking。本票不授权提交或发布。无固定层改动则保持 0.1.0。

---

## 归并错误与未决阻断

- **V08：** 选项正文未含记录位置/哈希范围；我在理由列的确认是规格阶段文字，**不是**他人同文投过的决议。幂等未实测（摘要不足 2）。
- **V15=B：** 「无可跑命令则记入 RELEASE」在命令为空时记什么不可执行。
- **V16=RULE × V04=NEEDED：** 「不得询问」vs 不可靠探测必须问。主持人不可拼接。
- **V12=LOCAL × 可审查性：** `.git/hooks` 不进版本库（B 论证；我未另测）。
- **V14：** 即使多数过 A 或 B，仍缺「hook 已被 Git 激活」的独立证据；不得用实跑成功宣称提交时会跑。
- **无**把摘要共同倾向当成已通过。归并对 A/B/C 编号同名的区分（选票 L5）忠实。

常规可在规格写清、**不**升级为维护者必答：标记字符串唯一拼写、指纹算法名、探测命令表、空 tracker 的「有效」实例。

---

## 开工准备度

**可以派规格票，不可派实现票，亦非必须维护者先裁（除非计票无两票或 V12/V13/V15 多数互斥）。**

规格交付：`docs/reviews/2026-09-05-lazy-setup/work/S1-spec/` 唯一新文件（已存在则停），含探测表、V08 标记语法+指纹位置/范围、V06 空文件实例、完成报告字段（过程 vs 四态 vs 激活证据）、TRACKED 接线与 hooksPath 确认项。验收只对照通过的选票正文+现行 DECISIONS，不发明固定层。实现票须等规格审查关闭上述阻断后再派。
