你是 lazypack-discipline 的 S1 规格编写者 B。三轮讨论已结束，这是落实已通过大方案的规格任务，不是第四轮讨论，也不是实现任务。请在原 B 会话继续。

工作仓库：<REPO_ROOT>

先阅读：
- README.md、docs/DECISIONS.md（现行为 0.2.0）、docs/interviews/2026-09-04-founding-interview.md。
- docs/reviews/2026-09-05-lazy-setup/vote.md。
- docs/reviews/2026-09-05-lazy-setup/synthesis.md，尤其 G01—G08。
- docs/reviews/2026-09-05-lazy-setup/round-3-ballot.md，只执行 vote.md 获多数的选项。
- 必要时读取三位 participants/*/round-3.md 的阻断项，个人建议不等于通过条文。

已收口的关键选择：V06=B 双检；V08=FINGERPRINT；V09=B 逐文件处理；V10=PRECISE；V11=REPORT；V12=TRACKED；V13=RECOMMEND；V14=A；V15=A；V07/V16=SKILL；V17=DEFER；V18=THIN。其余票见计票表。

特别纠正：你第三轮理由中“指纹相符的升级可不打扰用户”没有成为决议。V05=YES 要求有修改时整体确认一次；只有无变化的 no-op 不再确认。不要把个人补充当作同文多数，不要沿用已撤回的 GIT/CLEAN/REGENERATE。

目标：产出一份能供执行者照着实现、审查者按场景验收的 SPEC。核心机制给出一个具体方案，说明为何满足选票；不要给一串选项再把核心选择丢给实现者。常规工程细节由你在通过边界内确定，不再让维护者选算法名或标记字符串。

唯一允许在本仓新建的成果绝对路径：
../../../../.scratch\lazypack-setup\SPEC.md

写前检查，该文件若已存在则停止并报告主持人，不覆盖、不自行选 v2。可以创建必要父目录。此路径遵循固定层 §4.1 的规格草稿归属；未来关闭规格票前由主持人负责留证和清理，不由你自行删除。

SPEC 必须包含：
1. 目标、非目标、支持场景、精确输入输出清单和每项来源票号。
2. 用户流程：先探测、不重问 Matt、必要未知给推荐、一次整体确认、失败/拒绝/重复运行的具体完成语。无变化不要再确认。
3. G01—G08 逐项确定的契约，包括探测事实有效性、托管/未托管分类、块标记、指纹字节范围/编码/换行/元数据排除、模板标识和项目命令变化、缺损/无基线行为。
4. 六类产物的最小样例。只在 SPEC 的代码块内给示例，不创建生产模板。标明哪些已存在则追加、哪些应暂停、哪些允许改托管区，保留项目原有内容。
5. 受控 hook 的来源文件、激活方式、兼容已有 hook、跨设备克隆后启用；明确命令存在、命令运行结果、Git 实际调用 hook 三类证据如何分别取得。不能仅“写了文件”就称已激活。Windows/POSIX 的适用范围和未验证项须明说。
6. PRECISE+REPORT 下的恢复办法：已有暂存/未暂存改动、之后又被人改过的内容、本次新文件、半写入、git config/权限变更分别处理。不得以全局 checkout/reset/clean 或全盘回滚作答案。
7. 推荐默认工具时的依赖/配置/lockfile/Git 配置变更如何进入同一次确认；安装失败与运行失败的报告差异；拒绝安装仍诚实列未接线。
8. 离线快照和非本机绝对路径指针的具体解析；安装目录、源版本、模板生成标识不能悬空；不新增独立 CLI 或多份 YAML 状态层。
9. 验收矩阵：缺前置、空/损坏前置、已有人工文件、重复 no-op、块内外手改、命令变但版本不变、模板升级、标记损坏、部分写入、脏目标、已有 hook、空门禁、失败安装/检查、克隆后未激活、离线。每项输入、预期、验证办法、实测状态。
10. 执行任务边界与验收顺序，以及对应 G01—G08 的完成检查表。真正还没有答案的项明确标缺口，不为“可实现”伪造事实。

允许的验证活动（只验证规格疑问，不开发成品）：
- 读取本机实际 Matt skills；提供可追溯路径、版本/内容标识与短引文，不能把别人的实测当自己的。
- 可使用现有工具在以下唯一临时根做最小实验：
  <TEMP_DIR>/lazypack-setup-s1-spec-b-20260905
- 首次创建前检查该根不存在；若已存在则报告主持人，不覆盖。每个实验在其独立子目录，不使用真实项目、已有仓库或全局配置。
- 允许在该临时根内初始化无 remote 的空 Git 仓、写少量示例标记/hook 并运行诊断。禁止 git commit/push、创建正式包、发布、给真实项目安装依赖。实验只能修改临时仓本地配置，不能改用户全局配置。
- 不做递归删除/移动；实验遗留路径写在 SPEC 证据区，由主持人另行安排清理。
- 查 CLI/API 行为时遵守本仓 Context7 文档规则；本地实验以实际命令、退出结果和限制为准。不需要为了这一规格开发测试框架或独立工具。

写入禁区：
- 除 SPEC 与上述临时实验根之外，任何位置都不得写入。
- 不修改 DECISIONS、README、讨论原文、选票、计票、提示词、其他成果或 skills/。
- 不执行 git add/commit/push，不自行启动其他 AI 或创建后台服务。
- 只交 SPEC；没有本地文件能力则返回完整 Markdown 供主持人保存，不声称已写盘或实测。

完成后报告唯一成果路径、一句准备度结论，以及仍有无必须解决的阻断。随后由主持人基于你实际写出的稿件给 A/C 生成独立只读审查提示词，不直接开始实现。

