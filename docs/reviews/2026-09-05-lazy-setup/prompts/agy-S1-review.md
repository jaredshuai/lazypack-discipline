# 给 agy 的 S1 规格审查提示词

请在本地仓库 `<REPO_ROOT>` 做一次**只读规格审查**。

重要运行约束：

- 不要调用 `/understand-codebase`、codegraph、codebase-memory、fast-context 或任何 MCP；本次只读 Markdown 文件即可。
- 不要启动子代理、后台服务或浏览器。
- 不要创建、修改、删除、移动任何文件；不要执行 git add/commit/push。
- 不要把审查结果写入文件，直接在聊天中返回完整 Markdown 报告。
- 只使用本地文件读取和必要的只读命令；如果某个文件不存在，明确报告路径，不要猜测。

请读取以下文件：

1. `README.md`
2. `docs/DECISIONS.md`
3. `docs/interviews/2026-09-04-founding-interview.md`
4. `docs/reviews/2026-09-05-lazy-setup/vote.md`
5. `docs/reviews/2026-09-05-lazy-setup/synthesis.md`
6. `.scratch/lazypack-setup/SPEC.md`
7. `docs/reviews/2026-09-05-lazy-setup/round-3-ballot.md`

审查目标：判断 `.scratch/lazypack-setup/SPEC.md` 是否忠实实现已经通过的第三轮多数决议，并判断是否具备进入 S2 实现阶段的条件。

已经通过的关键选项是：

- V01 `YES`：prompt-driven setup skill，不做独立 CLI、YAML 状态层或常驻服务。
- V02 `ITEMS`：按六类产物控制范围，不机械限制为六个物理文件。
- V03 `YES`：固定层唯一可编辑源；skill 可带版本快照；目标项目不复制整份 DECISIONS。
- V04 `NEEDED`：默认零问，只问无法可靠探测且影响配置的必要未知，不设问题数量硬上限。
- V05 `YES`：一次整体展示和确认；无变化重跑不再确认。
- V06 `B`：Matt tracker 文件有效且常驻入口包含 Matt Agent skills 入口块，才算前置完成。
- V07 `SKILL`：常驻文件选择写在 setup skill 规格，不修改固定层 §1.3。
- V08 `FINGERPRINT`：托管区用指纹判断变化；版本戳不能单独证明没有人工修改。
- V09 `B`：无标记整份归属文件暂停该文件，其余可继续；不自动覆盖来源不明内容。
- V10 `PRECISE`：不因无关脏文件停止，只精确保护和修改本次目标路径。
- V11 `REPORT`：失败报告列出准确路径和差异，不承诺多文件原子写入，不使用全局清理或全局回滚。
- V12 `TRACKED`：hook 入口必须可被版本控制跟踪，不能只写 `.git/hooks`。
- V13 `RECOMMEND`：已有命令优先复用；代码仓完全缺命令时推荐默认工具，用户可拒绝并标记未接线。
- V14 `A`：setup 过程完成、命令接线、命令运行、Git hook 激活、检查通过必须分开报告。
- V15 `A`：固定层 §2.3 已增加：能接已有命令则写 hook，否则标明未接线或不适用及原因，不得宣称门禁已生效。
- V16 `SKILL`：lazy UX 写在 setup skill 和规格中，不修改固定层增加具体问答流程。
- V17 `DEFER`：不新增跨设备 rebase 规则，不修改 MCP 降级规则。
- V18 `THIN`：微信/DevEco 等平台只做薄草稿并标记未验证，不要求自动上传。
- V19 `YES`：先规格、再实现、再场景验收和独立审查；派工必须有唯一路径和写入边界。

请重点检查以下问题：

1. 前置双检是否与 Matt setup 的真实产物和 V06=B 一致，是否会无理由阻断合法文档仓。
2. `input-digest`、`gen`、`fp` 是否能支持跨会话重跑、模板升级、命令变化和人工修改检测。
3. 指纹的存放位置、哈希范围、编码、换行、头部元数据排除是否足够明确，是否存在自引用或永远 drift。
4. 无标记文件、标记损坏、部分生成文件、已有人工内容时，是否会误覆盖或错误继续。
5. V10=PRECISE 与 V11=REPORT 是否能保护已暂存、未暂存和后续人工修改；是否错误承诺 rollback。
6. V12=TRACKED 的 hook 方案是否明确兼容已有 Husky/其他管理器、Windows/POSIX、克隆后启用和 `core.hooksPath` 副作用。
7. V13/V14/V15 是否清楚区分命令缺失、安装失败、运行失败、hook 未激活和检查未通过；是否有虚假“已生效”表述。
8. 六类产物之间的依赖、暂停项和“部分完成”报告是否清楚。
9. 离线快照、skill 版本、源版本、目标项目指针是否可追溯且不依赖某台机器的绝对路径。
10. 验收矩阵是否覆盖票 V19 要求的场景，哪些仍只是设计文字而没有可验证办法。

输出格式：

```markdown
# S1 规格审查

## 结论
明确写：可以进入 S2 / 需要修订 SPEC / 阻断并需维护者裁决。

## P0
必须阻断实现的问题。没有就写“无”。每条含：文件:行号、触发场景、违反的 V 编号、为什么阻断、最小修复。

## P1
进入实现前应修复的问题。每条含：文件:行号、V 编号、风险、最小修复。

## P2
可在 S2 规格细化或实现后补足的问题。

## 已通过且落实正确的部分
只列有文件证据的内容。

## 未验证事项
区分“规格尚未验证”和“审查工具无法验证”。

## S2 准入条件
列出进入正式实现前必须满足的最多 5 条条件。
```

引用必须使用仓库相对路径和行号。不要重新设计整个方案，不要提出未通过投票的新固定层规则。
