# S3-SMOKE-1：原会话续跑与临时目录交接

在原 agy 隔离试用会话继续。你已准备 missing/happy fixture，并展示了 happy 的草案，当前停在真实维护者确认之前；这还不算试用完成。

源仓库：<REPO_ROOT>
原试用根：../../../../.scratch\lazypack-setup-s3-smoke-1\
先回读原任务 docs/reviews/2026-09-05-lazy-setup/prompts/agy-S3-smoke-1.md 与实际产品 SKILL.md，保留原权限和验收边界。唯一变更是报告改走以下系统临时目录；原试用根已经由你创建，应续用，不能按首次任务“目录存在”拒绝继续，也不能重新初始化/覆盖 baseline。

## 新的交接位置

预览唯一绝对路径：
<TEMP_DIR>/lazypack-discipline-handoff/smoke-1\preview.md

最终报告唯一绝对路径：
<TEMP_DIR>/lazypack-discipline-handoff/smoke-1\result.md

可创建必要父目录。两份文件初次创建时检查不存在；同名已存在且归属不明就报告，不覆盖、不自行改名。允许维护本轮自己创建的临时预览，在更新时注明原因；最终报告首次写入后不覆盖。主持人会直接读取以上路径，不需要维护者复制正文。

旧报告目标 docs/reviews/2026-09-05-lazy-setup/work/S3/agy-smoke-1.md 本次取消写入；若已意外生成，保留并报告，不移动、不删除、不覆盖。试验过程记录继续放原试用根，产品和所有历史资料只读。

## 从当前进度继续

1. 只读核对 baseline.json、plan.json、happy/missing 的实际文件与 Git 根。不要运行 init-fixtures.js 或重建 fixture，不把预览渲染脚本当成真实 skill 行为的替代实现。
2. 将当前拟写全文、现有 CLAUDE 的差异、PAUSE 项、元数据计算依据、hooksPath 副作用写到 preview.md。注明宿主和从文件加载 skill 的事实；不要把种子模板中的“已就绪”当已核实环境能力。
3. 这条续跑提示词只授权交接位置变化，绝不替代真实确认。若原会话尚无维护者对当前草案的明确确认，展示预览文件路径并仅询问一次是否写入；不要重复把整份草案粘贴在聊天中，也不要虚构用户回答。已有对同一草案的明确确认则沿用，不重复索要。
4. 收到真实确认后继续原试用的写盘、git hook run、本机二次重跑和字节核验。确认只覆盖原 happy 草案和其本地 Git 配置，不能扩大到源仓、全局配置、安装依赖或发布。
5. 二次运行从实际文件重新取值，不复用第一次内存中的摘要作为验证答案。既有 ARTIFACTS 的 PAUSE 可重报，但没有新变化不能再次要求确认或写盘。若失败，记录实际差异和原因，不私改产品或隐藏失败。
6. 核对 missing 零写入、happy 人工 ARTIFACTS 保留、条件指针省略、CLAUDE 块外字节/CRLF/哨兵保留、生成物无占位符、Git 原生调用与检查结果、二次 no-op、产品只读。不同证据层次分开记录，不以文件存在或模拟函数代替真实会话。
7. 写最终 result.md：结论通过/退回、真实确认请求和回复、实际操作与输出摘要、文件/配置前后对比、验证矩阵、未验证与限制、所有现场路径。不要把同会话重跑写成独立跨会话或多模型验证。

禁止 git add/commit/push/reset/clean、递归删除、产品修复、重跑旧套件、创建其他 AI/后台服务。允许执行的 hook 仅为原 happy fixture 中已确认的四项本地受控命令。

写 result.md 后，聊天只回复“完成”及该绝对路径。主持人将自行读取；后续留档和收尾由主持人安排，不要求维护者再粘贴报告。
