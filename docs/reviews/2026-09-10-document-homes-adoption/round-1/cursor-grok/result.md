# 文档归属正式采纳 R1：独立条文意见

日期：2026-09-10。席位：Cursor/Grok。输入：FIX2 候选 SHA256 `007f546921195a8fb7ca70ea22f0869ab6760326a5a7ccb57f038855c37626f3`（已复算吻合）。本轮是 §9.3 第一轮独立意见，不是终票，不实施，不写产品仓归档。

## 总体意见

FIX2 已把八区、六词、单入口、tracker 非 setup 产物、关票≠删除、清道夫不新增写进可审候选，可作为本轮条文讨论输入。咨询 R1–R3 与 F 轮外审不追认为选票。已知 `to-tickets:62` / `ask-matt:23` 写死 `.scratch/`，非默认路径未实跑；不得写成已适配或「必须等上游 PR」。本席对编号变更多数**保留候选**；对 2.3、3.4、4.1 耐久行、4.4 给精确修改。配套路径表/兼容说明的残留绝对句不进入固定层。

固定层改变的义务：1.1 使用非默认工单位置前须核消费者；2.3 登记实际映射并提供保留/迁移；3.1/3.4 清理须过四步关卡且不新增；4.1 拆耐久与临时、关票不自动删；4.4 迁移须整单审阅。未改词表不算新动议。具体落点（VISION、design、usage、evidence、interviews、ideas、实际 tracker 类型、是否 gitignore）留项目层 `docs/ARTIFACTS.md` Section 3。§4.1 原有 CONTEXT / adr / HISTORY / reference / research / 标准与发版家保持原文，不本轮重开。

## 逐项

### 1.1 — 保留候选

拟议全文保持 FIX2：

> 1.1 地基是 Matt Pocock 的 skills。本包只补它没管的部分，不替代、不并行复述。项目层用 `docs/agents/issue-tracker.md` 声明实际工单位置；使用非上游默认位置前，须核查开票、取票与更新消费者是否遵从该指引；存在写死路径时记为受限，不能只因指针已改就宣称适配完成；不侵入修改全局安装副本。

理由：规范目标与已然事实已分开，义务变化标「是」正确。主持勘误 3：适配途径不限于上游 PR；保留 `.scratch/`、改指针后人工核落点、或待上游改写，均是受限选项。1.1 正文未写「必须等 PR」，保持即可，勿把 result 叙事写进条文。

### 1.4 — 保留候选

拟议全文保持 FIX2 1.4（八区导航 + ARTIFACTS 登记 + 不强制预建目录）。

理由：八区是读档定位，不是安装清单。义务不增建文件。交叉引用：§1.3 单入口原文不动。

### 2.3 — 建议修改

候选漏了现行 PAUSE 与四槽闭合，写成「写出时须登记」会被读成写穿人工册。拟议全文：

> 2.3 **编译器** `/lazypack-setup`：在 `setup-matt-pocock-skills` 之后运行，问项目层问题，写出 `AGENTS.md`（或 `CLAUDE.md`）指针、`CODING_STANDARDS.md`、`RELEASE.md`、`docs/ARTIFACTS.md` 骨架、`docs/agents/roles.md`、提交前 hook。写出或更新 `docs/ARTIFACTS.md` 时须读取并登记 `docs/agents/issue-tracker.md` 及各逻辑区域已存在的实际物理原件；八区映射写入 Section 3，不改 Section 2 四槽；无原件不虚构登记行；登记册处于 PAUSE、BROKEN 或未托管时本轮不写穿，仅报告待裁。默认保留既有合理位置（保留模式），并提供迁至已登记或已按 1.1 核过的推荐路径的治理选项（迁移模式）。能接到已有 format/lint/type/test 命令则写出提交前 hook；否则标明未接线或不适用及原因，不得宣称门禁已生效。

影响：setup 多了「能写才写」与「映射进 Section 3」。不把 `docs/work/` 写成 setup 必须迁入的家。

### 清道夫行（§3.1）— 保留候选

拟议全文保持 FIX2 表行：输出改为「回收临时现场与缓存、归档废弃产物、清死分支」；允许写「仅在通过四步清理关卡后删除或移动，不新增」。

理由：废除「删整个 `.scratch/`」；登记仍只读。E02 由 setup 首次 / 书记员维护，与此一致。交叉引用：规划者/书记员「允许写」原文不动。

### 3.4 — 建议修改

关卡正文保留；补回候选漏掉的「事件驱动，不用定时器」（0.2.0 原句，delta 引用了旧文却未写入新文）。拟议全文：

> 3.4 书记员在每张票关闭后扫一遍；清道夫在发版前或按需扫一遍。事件驱动，不用定时器。清道夫执行清理时必须严格遵守**四步清理关卡**：①排查是否在登记册、工单配置或 `.out-of-scope/` 中声明为权威原件或持久来源；命中登记或权威原件者不得当作可丢弃缓存自动清除，是否归档或删除仍依保留目的、有效引用与特定授权判断；②核验内容是否处于活跃状态（读取项目 `issue-tracker.md` 与 `triage-labels.md` 实际声明的状态词，不写死状态字符串；状态无法确定时报告未知并暂停自动清理，不改写原状态，不擅自视为活跃）；③按保留目的与复现条件鉴别是否为可丢弃缓存（严禁仅凭文件名或目录名推定，严禁将 specs、issues、out-of-scope 记录视为临时缓存）；④存在争议或未分类文件原地保留并列清单请用户裁定，裁决前不移动、不归档。严禁仅凭目录名或关票事件一键物理清空。

理由：Gate1/2 已对齐主持勘误 2 与 C5。`.out-of-scope/` 按此条：不得当缓存自动清，不新增「永不可删」义务。ADR「不删」仍只在 §4.1 ADR 行。路径表 Z08 / 场景 12「绝不删除」不得当作已确认固定层。

### 3.5 — 保留候选

拟议全文保持：「清道夫 | 暂无 skill，靠条文与四步清理关卡」。形式对应，无新自动化承诺。

### 4.1 耐久行 — 建议修改

反对把 `docs/work/<feature>/` 写成固定层「标准默认」家。原则（拆耐久/临时、关票不自动删）保留。拟议表行：

> | 需求、工单与规格 | 以 `docs/agents/issue-tracker.md` 声明的实际位置为准；未声明时沿用地基本地默认 `.scratch/<feature>/`。`docs/work/<feature>/` 是需按 1.1 核查后才采用的推荐耐久落点，核查未通过前不得当作已生效的家 | 按保留目的与引用判断；关票不自动构成删除授权 |

理由：本机开票消费者仍写 `.scratch/`。固定层应收「指针权威 + 关票不删 + 推荐须核」；具体目录字符串留项目层。外部 tracker 不迁、不复制正文。不得要求本轮为 `docs/work/` 改上游或跑产品。

### 4.1 临时行 — 保留候选

> | 临时现场与运行时缓存 | `.scratch/<feature>/` | 票关闭且验证交付后，经四步清理关卡安全回收 |

与耐久行拆开后，`.scratch/` 不再同时是唯一工单家和必删草稿箱。gitignore 不否定权威（见路径表 C5 说明，不写入本行）。

### 4.2 — 保留候选

拟议全文保持 FIX2 4.2。六词不是新动议。后两句（移动不改效力、关票≠删除授权）是对 4.1 的效力说明，义务已在 4.1 改变，4.2 不另增词表。

### 4.4 — 建议修改

候选未写 PAUSE 与工单迁移仍受 1.1 约束。拟议全文：

> 4.4 存量项目的非标路径在初始化时默认保留；能安全写入登记册时，在 `docs/ARTIFACTS.md` Section 3 登记实际路径与章节。治理迁移选项触发时，AI 须整体输出源路径至目标路径的映射清单、链接与附件影响评估，经用户整体审阅确认后执行，不逐文件打扰。登记册处于 PAUSE、BROKEN 或未托管时只报告不写穿。工单类迁移另受 1.1 消费者核查约束；未通过则保持保留模式。

谁在每个事件同步 Section 3、如何做链接检查，仍是实施规格，本条不宣称已自动化（主持勘误 4）。

### 5.5 — 保留候选

拟议全文保持 FIX2 5.5。CHANGELOG 是发布派生记录，验收留工单/规格。不改 Keep a Changelog 格式义务。

## 主持勘误处理

1. **fp / input**：同意勘误。`compatibility.md` L114「fp 与旧输入吻合」不采纳。算法只以现行 `managed-blocks.md` 决策树为准：`current_fp` 对 `header.fp`，`current_input` 对 `header.input`；UPGRADE/DRIFT/CONFLICT 仅在该仓重跑 setup 时评估。不从报告发明新算法。不写入 DECISIONS。
2. **`.out-of-scope/` 绝对保留**：不同意把路径表「绝不删除」升格为新固定层义务。一致限定用本席 3.4 Gate1 拟议文。历史 ADR 不删仍按原 §4.1 ADR 行。
3. **非默认 tracker 受限**：同意。1.1 已够。禁止把「必须等上游 PR」写进条文或当成唯一适配。本机事实是写死；非默认位置未验证。

## 保留 / 迁移 / 耐久路径（一条规则）

未获治理授权：保留现有正文与 tracker 声明位置，能写则在 Section 3 登记，不能写则 PAUSE 报告。用户批准整单迁移：只迁已核消费者的类别；外部 tracker 默认不迁。新推荐 `docs/work/` 仅在 1.1 通过后作为该项目耐久家；否则 `.scratch/` 或外部系统仍是权威，靠四步关卡防误清。地基缺口保持受限，不本轮实跑、不解锁实施票。

## 须在互评解决的分歧（三项）

1. §4.1 耐久「家」列：是否允许固定层出现 `docs/work/` 作为「标准默认」，还是必须写成「1.1 通过后的推荐落点」。
2. §2.3 / §4.4：PAUSE 与 Section 3 是否写入固定层，还是只留 setup 正文。
3. §3.4：漏写的「事件驱动，不用定时器」是笔误应恢复，还是有意放宽为可定时扫描。

普通目录拼写不重开。本轮未完成 `docs/reviews/2026-09-10-document-homes-adoption/` 归档，不能声称流程闭环或规则生效。
